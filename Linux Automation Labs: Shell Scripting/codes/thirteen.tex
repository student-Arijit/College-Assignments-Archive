\begin{verbatim}
if [ $# -ne 1 ]; then
    echo "Usage: $0 filename"
    exit 1
fi
file=$1
if [ ! -f "$file" ]; then
    echo "Error: File '$file' not found!"
    exit 2
fi
tr -s '[:space:][:punct:]' '\n' < "$file" | tr 'A-Z' 'a-z' | 
grep -v '^$' | sort | uniq -c | sort -nr
\end{verbatim}