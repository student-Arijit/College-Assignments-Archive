\begin{verbatim}
if [ $# -ne 1 ]; then
    echo "Usage: $0 range"
    exit 1
fi
n=$1
a=0
b=1
echo -n "Fibonacci series up to $n: "
while [ $a -le $n ]; do
    echo -n "$a "
    fn=$((a + b))
    a=$b
    b=$fn
done
echo
\end{verbatim}