\begin{verbatim}
[ $# -ne 1 ] && echo "Usage: $0 filename" && exit 1
[ ! -f "$1" ] && echo "File not found!" && exit 2
lines=$(wc -l < "$1")
words=$(wc -w < "$1")
chars=$(wc -m < "$1")
echo "$lines $words $chars $1"
\end{verbatim}