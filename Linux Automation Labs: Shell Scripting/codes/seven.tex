\begin{verbatim}
if [ $# -ne 1 ]; then
    echo "Usage: $0 folder_name"
    exit 1
fi
folder=$1
if [ ! -d "$folder" ]; then
    echo "Error: '$folder' is not a directory!"
    exit 2
fi
echo "Empty subfolders listed in 'empty_subfolders.txt'"
\end{verbatim}