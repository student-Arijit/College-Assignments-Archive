\begin{verbatim}
class CarAcceleration:
    def calculate_acceleration(self):
        v_initial = 0
        v_final = 10
        time = 20
        acceleration = (v_final - v_initial) / time
        return acceleration

car = CarAcceleration()
print("Acceleration:", car.calculate_acceleration(), "m/s²")
\end{verbatim}