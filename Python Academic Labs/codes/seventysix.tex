\begin{verbatim}
import numpy as np
# Define a 3x3 matrix
A = np.array([
    [4, 2, 1],
    [0, 3, -1],
    [0, 0, 2]
])
# Compute eigenvalues
eigenvalues = np.linalg.eigvals(A)
# Display result
print("Matrix A:")
print(A)
print("Eigenvalues:")
print(eigenvalues)
\end{verbatim}