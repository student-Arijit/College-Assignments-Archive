\begin{algorithm}
\caption{Create and Display a Pandas DataFrame with Index Labels}
\begin{algorithmic}[1]

\STATE Import Pandas library

\STATE Define dictionary data:
\STATE $data \gets \{Name, Age, Marks\}$

\STATE Create DataFrame with index labels:
\STATE $df \gets DataFrame(data,\ index = [S1, S2, S3, S4])$

\STATE Display message:
\STATE Output ``DataFrame with index labels:''

\STATE Display DataFrame:
\STATE Output $df$

\end{algorithmic}
\end{algorithm}