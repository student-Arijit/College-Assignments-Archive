\begin{algorithm}
\caption{Find Numbers Containing 1 to 3 Digits in a String}
\begin{algorithmic}[1]

\STATE Input a string:
\STATE $text \gets$ user input

\STATE Create a pattern to match numbers containing 1 to 3 digits

\STATE Search the string for all matching numbers:
\STATE $result \gets$ all matches found in $text$

\STATE Output ``Numbers (1 to 3 digits): '' $result$

\end{algorithmic}
\end{algorithm}