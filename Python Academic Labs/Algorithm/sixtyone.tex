\begin{algorithm}
\caption{Abbreviate "Street" to "St." in a Given String}
\begin{algorithmic}[1]

\STATE Input a string:
\STATE $text \gets$ user input

\STATE Define pattern to match the word ``Street''

\STATE Replace all occurrences of ``Street'' with ``St.'':
\STATE $updated\_text \gets$ modified string after replacement

\STATE Output the result:
\STATE ``Result: '' $updated\_text$

\end{algorithmic}
\end{algorithm}